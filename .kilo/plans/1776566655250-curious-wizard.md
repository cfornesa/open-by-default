# Plan: Add Six Tool Cards to Tools Section

## Overview
Add six tool cards with Lorem Ipsum text to the "Tools" section of index.html, implementing a responsive grid layout that shows 3 cards per row on desktop, 2 cards per row on tablet, and 1 card per row on mobile. Each card will feature an icon decoration before the header and a one-sentence paragraph.

## Implementation Plan

### 1. HTML Structure Changes
**File:** `/Users/Fornesus/Code/open-by-default/index.html`

**Location:** Lines 92-94 (inside `.tools-grid` div)

**Add six tool card elements with the following structure:**

```html
<div class="tool-card">
  <header class="tool-header">
    <span class="tool-icon" aria-hidden="true">🔧</span>
    <h3>Tool One</h3>
  </header>
  <div class="tool-content">
    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
  </div>
</div>
```

**Six cards with unique icons and content:**
1. 🔧 - "Tool One" - "Lorem ipsum dolor sit amet, consectetur adipiscing elit."
2. 🛠️ - "Tool Two" - "Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
3. 🚀 - "Tool Three" - "Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris."
4. 💻 - "Tool Four" - "Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore."
5. 🔒 - "Tool Five" - "Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia."
6. 🔍 - "Tool Six" - "Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium."

### 2. CSS Styling Changes
**File:** `/Users/Fornesus/Code/open-by-default/css/main.css`

**Add new CSS rules for tool cards:**

#### Tool Card Base Styles (Lines ~780+)
```css
/* Tool Cards - Responsive Grid Layout */
.tool-card {
  border: 3px solid var(--color-border-primary);
  border-radius: 12px;
  background: linear-gradient(135deg, var(--color-bg) 20%, var(--color-accent-black) 80%);
  box-shadow: 0 8px 24px rgba(255, 77, 77, 0.3);
  transition: transform var(--transition-base), box-shadow var(--transition-base);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  min-height: 200px;
}

.tool-card:hover,
.tool-card:focus {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(255, 77, 77, 0.1);
  border-color: var(--color-primary-1);
}

.tool-header {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-md);
  background: linear-gradient(135deg, var(--color-primary-1), var(--color-primary-2));
  border-bottom: 2px solid var(--color-primary-2);
}

.tool-icon {
  font-size: 1.5rem;
  color: var(--color-accent-white);
  flex-shrink: 0;
  line-height: 1;
}

.tool-card h3 {
  font-family: var(--font-heading);
  font-size: 1.25rem;
  font-weight: 600;
  color: var(--color-accent-white);
  margin: 0;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
}

.tool-content {
  padding: var(--space-md);
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
}

.tool-content p {
  margin: 0;
  text-align: center;
  color: var(--color-muted);
  line-height: 1.5;
}
```

#### Responsive Grid Layout (Lines ~810+)
```css
/* Desktop - 3 columns (1024px+)
@media (min-width: 1024px) {
  .tools-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* Tablet - 2 columns (768px - 1023px) */
@media (min-width: 768px) and (max-width: 1023px) {
  .tools-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Mobile - 1 column (767px and below) */
@media (max-width: 767px) {
  .tools-grid {
    grid-template-columns: 1fr;
  }
}
```

### 3. Implementation Details

**Grid Layout Strategy:**
- Use CSS Grid with `grid-template-columns` media queries
- Maintain consistent gap spacing with `gap: var(--space-xl)`
- Ensure cards have equal height with `min-height` and flexbox

**Icon Selection:**
- Use emoji icons for visual appeal and accessibility
- Include `aria-hidden="true"` on icon spans
- Icons chosen for tool-related themes

**Content Structure:**
- One-sentence paragraphs for concise presentation
- Center-aligned text within cards
- Consistent styling with existing design system

### 4. File Changes Summary

**index.html:**
- Add 6 tool card elements inside `.tools-grid` div
- Each card contains header with icon and title, plus content paragraph

**main.css:**
- Add tool card base styles (border, background, shadow, transitions)
- Add tool card header styles (gradient background, icon styling)
- Add tool card content styles (centering, text alignment)
- Add responsive grid media queries

### 5. Testing Considerations

**Cross-device testing:**
- Desktop: 3 cards per row, proper spacing
- Tablet: 2 cards per row, proper spacing
- Mobile: 1 card per row, full width

**Accessibility:**
- Semantic HTML structure
- Proper heading hierarchy
- Focus states on interactive elements
- ARIA labels where appropriate

**Visual consistency:**
- Matches existing design system colors
- Consistent spacing and typography
- Smooth hover transitions

## Questions for Clarification

1. **Icon preference:** Should I use the emoji icons I've planned, or would you prefer different icons or a different icon approach?

2. **Content length:** Are the one-sentence Lorem Ipsum paragraphs appropriate, or would you prefer longer content?

3. **Card height:** Should all cards have equal height (using flexbox), or should they be content-based height?

4. **Interaction:** Should the cards be clickable links to tool pages, or purely decorative display elements?

Once you approve this plan, I'll proceed with implementation.

---

**Plan Status:** Complete
**Ready for implementation:** Yes
**Questions remaining:** 4