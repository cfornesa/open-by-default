# Plan: Add Two Lorem Ipsum Articles with Creative Borders

## Goal
Add a new div section to index.html containing two Lorem Ipsum articles, each with a creatively styled border that makes them replicable and visually distinct.

## Current State Analysis
- index.html has a hero section, tools section, and footer
- The tools section has a grid layout for tool cards
- CSS has an extensive border styling system with experimental colors

## Implementation Plan

### 1. HTML Structure Addition
Add a new section between the hero and tools sections:
```html
<section aria-labelledby="articles-heading">
  <div class="section-header">
    <h2 id="articles-heading">Articles</h2>
  </div>
  <div class="articles-container">
    <article class="article-item">
      <h3>Article One</h3>
      <p>Lorem ipsum dolor sit amet...</p>
    </article>
    <article class="article-item">
      <h3>Article Two</h3>
      <p>Lorem ipsum dolor sit amet...</p>
    </article>
  </div>
</section>
```

### 2. CSS Styling Additions
Add to main.css:

```css
/* Articles Section */
.articles-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 var(--space-lg);
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-xl);
  margin-bottom: var(--space-3xl);
}

.article-item {
  background: linear-gradient(135deg, var(--color-bg) 20%, var(--color-accent-black) 80%);
  padding: var(--space-xl);
  border-radius: 12px;
  border: 4px solid var(--color-border-primary);
  position: relative;
  overflow: hidden;
  transition: transform var(--transition-base), box-shadow var(--transition-base);
}

.article-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, var(--color-primary-1), var(--color-primary-2), var(--color-primary-3));
}

.article-item h3 {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 600;
  margin-bottom: var(--space-md);
  color: var(--color-primary-1);
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
}

.article-item p {
  color: var(--color-muted);
  line-height: 1.6;
  margin-bottom: 0;
}

.article-item:hover,
.article-item:focus {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(255, 77, 77, 0.2);
  border-color: var(--color-primary-2);
}

.article-item:focus {
  outline: 2px solid var(--color-primary-1);
  outline-offset: 2px;
}

/* Mobile Responsive */
@media (max-width: 767px) {
  .articles-container {
    padding: 0 var(--space-lg);
    gap: var(--space-lg);
  }
  
  .article-item {
    padding: var(--space-lg);
  }
}
```

### 3. Design Considerations
- **Creative Borders**: Use gradient borders with the existing tri-color scheme
- **Replicability**: Simple class-based structure that can be copied
- **Visual Hierarchy**: Consistent with existing design language
- **Accessibility**: Proper semantic HTML with ARIA labels
- **Responsive**: Grid layout that adapts to mobile screens

### 4. Integration Points
- Insert after line 56 (after hero section closes)
- Add CSS at the end of main.css (before responsive breakpoints)
- Use existing color variables for consistency
- Follow existing spacing and typography patterns

### 5. Testing Requirements
- Verify responsive behavior on mobile/tablet/desktop
- Check border styling and hover states
- Ensure accessibility compliance
- Validate HTML structure

## Implementation Details

### Article Content
- **Article 1**: Long Lorem Ipsum text (multiple paragraphs)
- **Article 2**: Single paragraph of Lorem Ipsum text
- Text will be different for each article

### Visual Elements
- Each article header will be preceded by an icon
- Icons will use emoji or SVG icons that match the existing design aesthetic

### Mobile Layout
- Articles will be horizontally scrollable on mobile instead of stacked
- This requires a different CSS approach using `overflow-x: auto` and flexbox

### Updated HTML Structure
```html
<section aria-labelledby="articles-heading">
  <div class="section-header">
    <h2 id="articles-heading">Articles</h2>
  </div>
  <div class="articles-container mobile-scroll">
    <article class="article-item">
      <header class="article-header">
        <span class="article-icon" aria-hidden="true">ℹ️</span>
        <h3>In-Depth Analysis</h3>
      </header>
      <div class="article-content">
        <p>Long Lorem ipsum text...</p>
        <p>Additional paragraphs...</p>
        <p>More detailed content...</p>
      </div>
    </article>
    <article class="article-item">
      <header class="article-header">
        <span class="article-icon" aria-hidden="true">✨</span>
        <h3>Quick Insight</h3>
      </header>
      <div class="article-content">
        <p>Single paragraph Lorem ipsum text...</p>
      </div>
    </article>
  </div>
</section>
```

### Updated CSS Requirements
```css
/* Articles Section - Mobile Scroll */
.articles-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 var(--space-lg);
  margin-bottom: var(--space-3xl);
}

.mobile-scroll {
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch;
  padding-bottom: var(--space-lg); /* For scrollbar visibility */
}

.mobile-scroll .article-item {
  scroll-snap-align: start;
  min-width: 90vw; /* Ensure full screen width on mobile */
  margin-right: var(--space-lg);
}

/* Enhanced Article Header */
.article-header {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  margin-bottom: var(--space-md);
}

.article-icon {
  font-size: 1.5rem;
  color: var(--color-primary-1);
  flex-shrink: 0;
}

.article-item h3 {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--color-primary-1);
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
  margin: 0;
}

/* Enhanced Article Content */
.article-content {
  line-height: 1.6;
  color: var(--color-muted);
}

.article-content p {
  margin-bottom: var(--space-md);
}

.article-content p:last-child {
  margin-bottom: 0;
}

/* Mobile Scroll Enhancements */
@media (max-width: 767px) {
  .mobile-scroll {
    padding-bottom: var(--space-md);
  }
  
  .mobile-scroll .article-item {
    min-width: 100vw;
    padding: var(--space-lg);
    border-radius: 0;
    border-left: none;
    border-right: none;
  }
  
  .articles-container {
    padding: 0;
  }
}

/* Desktop Grid Fallback */
@media (min-width: 768px) {
  .mobile-scroll {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    overflow-x: visible;
  }
  
  .mobile-scroll .article-item {
    min-width: auto;
    margin-right: 0;
  }
}
```

### Icon Selection
- Article 1: ℹ️ (information) - for in-depth analysis
- Article 2: ✨ (sparkles) - for quick insight

### Lorem Ipsum Content
- **Article 1**: Extended Lorem Ipsum with 3-4 paragraphs for depth
- **Article 2**: Single concise paragraph for quick reading

### Integration Points
- Insert after line 56 in index.html
- Add CSS at the end of main.css (before responsive breakpoints)
- Use existing color variables and spacing patterns
- Maintain accessibility with proper ARIA labels

## Files to Modify
- `/Users/Fornesus/Code/open-by-default/index.html` - Add new section with mobile-scroll class
- `/Users/Fornesus/Code/open-by-default/css/main.css` - Add article styling with mobile scroll enhancements

## Testing Requirements
- Verify horizontal scrolling works on mobile devices
- Test scroll snap behavior
- Check responsive grid on desktop/tablet
- Validate icon display and accessibility
- Ensure border styling remains consistent
- Test keyboard navigation and focus states

## Files to Modify
- `/Users/Fornesus/Code/open-by-default/index.html` - Add new section
- `/Users/Fornesus/Code/open-by-default/css/main.css` - Add article styling

## Implementation Order
1. Add HTML section to index.html
2. Add CSS styling to main.css
3. Test responsive behavior
4. Validate accessibility

Ready for implementation once questions are answered and plan is approved.