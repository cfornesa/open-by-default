# Plan: Create articles/index.html with Tools Sections Only

## Objective
Create a new index.html file in the `articles/` subfolder that contains only the two "tools" sections (Free Tools and Paid Tools) from the root index.html, with appropriate modifications for the guides context.

## Analysis of Root index.html
Based on the current index.html structure, the two "tools" sections are:
1. **Free Tools** (lines 88-143)
2. **Paid Tools** (lines 145-200)

## Key Requirements
- New file location: `articles/index.html`
- Only include the two tools sections (Free Tools and Paid Tools)
- Remove the "Articles" sections (lines 58-86 and 202-230)
- Remove the hero section (lines 50-56)
- Update navigation to reflect "Guides" context
- Maintain all existing CSS, JS, and semantic HTML structure
- Preserve accessibility features (ARIA labels, semantic tags)

## Plan Steps

### 1. File Structure Setup
- Create `articles/` directory if it doesn't exist
- Create new `articles/index.html` file

### 2. HTML Template Structure
- Copy the HTML boilerplate from root index.html (DOCTYPE, html, head, body)
- Update the page title to reflect "Guides" context
- Update the meta description to be more guide-specific
- Keep all CSS and font links identical

### 3. Navigation Updates
- Update the navigation to show "Guides" as the active page
- Keep all other navigation links (Home, Tools, About)
- Update the site title link to point to the correct location

### 4. Content Structure
- Remove hero section (lines 50-56)
- Remove both "Articles" sections (lines 58-86 and 202-230)
- Keep only:
  - Free Tools section (lines 88-143)
  - Paid Tools section (lines 145-200)

### 5. Content Refinements
- Consider updating section headings to better reflect "Guides" context
- Verify all internal links and references are correct
- Ensure accessibility features are maintained

### 6. Validation
- Verify HTML structure is valid
- Test that all CSS and JS links work correctly
- Ensure navigation functions properly
- Confirm accessibility features are intact

## Questions for Clarification

1. **Content Context**: Should the tools sections in the guides page have any different content or descriptions compared to the main page? The user mentioned "main content should only contain the two tools sections" but didn't specify if the content itself should be modified.

2. **Navigation Behavior**: Should the "Guides" navigation item be marked as active/selected in the articles/index.html file?

3. **File Dependencies**: Are there any specific CSS classes or JavaScript functionality that should be added or modified for the guides context?

4. **Future Content**: Is this intended to be a static page with just the tools sections, or should it be designed to accommodate additional guide content in the future?

## Implementation Considerations

- **File Path**: The new file will be at `articles/index.html`
- **Relative Links**: All CSS, JS, and asset links should work correctly since they're relative to the root
- **Navigation**: The articles page will link back to the main tools page at `/tools/`
- **Accessibility**: Maintain all existing ARIA labels and semantic HTML structure

## Success Criteria
- New `articles/index.html` file exists and is properly structured
- Contains only the two tools sections (Free Tools and Paid Tools)
- All styling and functionality works correctly
- Navigation reflects the guides context appropriately
- File is accessible and follows semantic HTML best practices

## Next Steps
Once the plan is approved, I will:
1. Create the `articles/` directory
2. Generate the `articles/index.html` file with the specified content
3. Test the file to ensure all links and functionality work correctly

Please confirm if this plan meets your requirements or if you'd like any modifications before implementation.